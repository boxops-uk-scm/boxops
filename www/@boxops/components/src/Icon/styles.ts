import * as stylex from '@stylexjs/stylex';

export const styles = stylex.create({
  base: {
    fill: 'inherit',
    color: 'inherit',
  },
  container: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
