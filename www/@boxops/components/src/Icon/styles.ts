import * as stylex from '@stylexjs/stylex';

export const styles = stylex.create({
  base: {
    fill: 'inherit',
    color: 'inherit',
  },
  container: {
    display: 'flex',
    width: 'fit-content',
    height: 'fit-content',
  },
});
