import * as internal from './ButtonGroup';
import { styles } from './styles';

const ButtonGroup = Object.assign(internal.default, {
  styles,
});

namespace ButtonGroup {
  export type Props = internal.Props;
}

export default ButtonGroup;
